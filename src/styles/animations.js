import { keyframes } from 'styled-components';

export const showOpacity = keyframes`
  to {
    opacity: 1;
  }
`;

export const showTranslate = keyframes`
  to {
    transform: none;
    opacity: 1;
  }
`;
